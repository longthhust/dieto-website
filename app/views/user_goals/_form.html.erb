<%= form_for(@user_goal) do |f| %>
  <% if @user_goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_goal.errors.count, "error") %> prohibited this user_goal from being saved:</h2>

      <ul>
      <% @user_goal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "目標の体重" %><br>
    <%= f.number_field :weight %>　kg
  </div>
  
  <div class="field">
    <%= f.label "締め切り" %><br>
    <%= f.date_select :deadline %>
  </div>
  
  <div class="field">
    <%= f.label "コメント"%><br>
    <%= f.text_area :comment %>
  </div>
  
  <div class="actions">
    <%= f.submit "目標作成"%>
  </div>
<% end %>
