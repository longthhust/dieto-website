<%= form_for(@user_goal) do |f| %>
  <% if @user_goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_goal.errors.count, "error") %> prohibited this user_goal from being saved:</h2>

      <ul>
      <% @user_goal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
      <label for="title">タイトル</label>
      <%= f.text_field :title %>
  </div>
  <div class="form-group">
      <label for="weight">目標の体重</label>
      <%= f.number_field :weight %>
  </div>
  <div class="form-group">
      <label for="deadline">締め切り</label>
      <%= f.date_select :deadline, class: 'form-control'%>
  </div>
  <div class="form-group">
      <label for="comment">コメント</label>
      <%= f.text_area :comment, class: 'form-control', placeholder: 'Your comment'%>
  </div>
  <div class="actions">
  <%= f.submit "目標作成", class: 'btn btn-block btn-primary btn-sm' %>
  </div>
<% end %>