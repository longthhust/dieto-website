  <nav id="myNavbar" class="navbar navbar-static-top bs-docs-nav navbar-inverse role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbarCollapse">
                <span class="sr-only"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="<%= root_path %>">ダイエット</a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="nav navbar-nav navbar-right">
              <% if user_signed_in? %>
              <li>
                  <%= form_tag(search_path, :method => "post", class: "navbar-form navbar-left", id: "search-form") do %>
                    <div class="input-append">
                      <input type="text" class="form-control" placeholder="Search" name="name" value="<%= @keyword%>">
                      <!-- In order to have the "search" icon int the button, we need to use plain HTML instead 
                           of using a Rails form helper -->
                      <button class="btn" type="submit"><i class="glyphicon glyphicon-search"></i></button>
                    </div>
                  <% end %>
                <!--<form class="navbar-form navbar-left" role="search" method="post" action"">-->
                <!--  <div class="form-group">-->
                <!--    <input type="text" class="form-control" placeholder="Search">-->
                <!--    <button type="submit" class="btn btn-default"><i class="glyphicon glyphicon-search"></i></button>-->
                <!--  </div>-->
                  
                <!--</form>-->
              </li>
              <li><%= link_to "Hello, #{current_user.email}!", current_user %></li>
              <li><%= link_to "目標一覧", user_goals_path %></li>
              <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>
              <% else %>
              <li><%= link_to "ログイン", new_user_session_path %></li>
              <li><%= link_to "ユーザー登録", new_user_registration_path %></li>
              <% end %>
            </ul>
        </div>
    </div>
</nav>
  