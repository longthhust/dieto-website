<%= form_for(@user_log) do |f| %>
  <% if @user_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_log.errors.count, "error") %> prohibited this user_log from being saved:</h2>

      <ul>
      <% @user_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
      <label for="weight">現在の体重</label>
      <%= f.number_field :weight %>
  </div>
  <div class="form-group">
      <label for="comment">コメント</label>
      <%= f.text_area :comment, class: 'form-control', placeholder: 'Your comment'%>
  </div>
  <div class="actions">
  <%= f.submit "作成", class: 'btn btn-block btn-primary btn-sm' %>
  </div>
<% end %>